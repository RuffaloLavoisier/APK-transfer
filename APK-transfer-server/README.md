# APK-transfer-SERVER

This is the server component of APK-transfer, which works in conjunction with the APK-transfer-APP to facilitate APK file transfers from an Android device to a PC.

## Running the Server

To run the server, execute the following command:

```
$ python main.py
```